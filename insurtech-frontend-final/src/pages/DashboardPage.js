import React from 'react';

// 임시 스텁: App.js가 import 하지만 실제 파일이 누락돼 빌드가 실패하던 상태였음.
// SafetyLandingPage 도입 시 빌드 통과를 위해 placeholder로 추가. 추후 실제 대시보드로 교체하세요.
export default function DashboardPage() {
  return (
    <div style={{ padding: 32, fontFamily: 'system-ui' }}>
      <h1>Dashboard (placeholder)</h1>
      <p>이 페이지는 임시 스텁입니다.</p>
    </div>
  );
}
